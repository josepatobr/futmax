[project]
name = "futmax"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "bcrypt>=4.2.1",
    "django>=5.1.6",
    "pillow>=11.1.0",
    "python-decouple>=3.8",
]
